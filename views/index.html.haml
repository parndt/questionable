!!!
%html
  %head
    %link{rel: 'stylesheet', href: 'ui.css'}
    %script{src: 'jquery-min.js'}
    %script{src: 'jquery-ui-custom-min.js'}
    :javascript
      $(document).ready(function(){
        $('#tabs').tabs();
      });
  %body
    #tabs
      %ul#nav.clearfix
        - comics.map do |comic|
          %li
            %a{href: "##{comic.haml_object_ref}_#{comic.title}"}
              %span= comic.title
      %br
      - comics.map do |comic|
        %div[comic]= comic.images.flatten.join('<br/>')
